apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-polaris-ui.fullname" . }}
  labels:
    {{- include "apache-polaris-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apache-polaris-ui.selectorLabels" . | nindent 4 }}

